# Hengshu v1.0.0.44

## 这版解决什么

- 删除请求/观察监控入口、监控实现和悬浮层；不创建后台隐藏监控任务。
- 复杂度判断改为结构化维度，中文低置信度长任务有保守升级路径。
- 增加 `auto`、`fast`、`standard`、`deep` 四档和总量/并发预算。
- 精简 11 个角色的重复指令，加入受限 PowerShell 标量优先降级。
- 任务结束提供一次性验收摘要，区分 requested、observed 和 unknown。

## 验证

- 核心测试 `208/208` 通过。
- Release x64 构建 `0` 警告、`0` 错误。
- MSIX 签名、版本契约和 23 个负例通过。
- 分享包 37 个文件、禁止文件计数 0，SHA-256：
  `00932924CB7D15786C36ED293A72200CEA7B3D42956068470279C13DC03CD497`。
- 已安装态 WinUI 三种布局回归通过，已移除监控/悬浮层入口计数为 0。
- 五个复杂度/调度档计划案例通过；11 个角色配置安装后逐文件哈希一致。

## 运行时边界

CLI 只生成原生派发计划，不在自身启动子智能体。Commander 继承当前 Codex 对话框的模型和推理强度；角色路由是请求配置，原生运行时未回显的实际字段必须保持 unknown。
